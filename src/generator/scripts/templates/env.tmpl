################################################################################
# ⚠️  SECURITY WARNING — READ BEFORE USING
#
# This file contains DEVELOPMENT-ONLY placeholder credentials generated by
# Hyperledger Fabric Network Builder. Every password below is a weak default.
#
# BEFORE deploying to any shared, staging, or production environment:
#   1. Replace ALL passwords with strong, randomly generated values (≥ 32 chars).
#   2. Ensure this file is NOT committed to version control (.gitignore covers it,
#      but verify before running `git add .`).
#   3. For production, migrate secrets to a proper secrets manager:
#      HashiCorp Vault, AWS Secrets Manager, or Azure Key Vault.
#   4. Set restrictive permissions: chmod 600 .env
#
# This file is auto-generated. Manual edits will be overwritten on regeneration.
################################################################################
# Shared secrets for the Hyperledger Fabric network
{{- range $i, $org := .Orgs }}
{{- $orgUpper := toUpper $org.Name }}

# {{ $org.Name }} CA Credentials
{{ $orgUpper }}_CA_ADMIN_USER=admin
{{ $orgUpper }}_CA_ADMIN_PASSWORD=adminpw
{{ $orgUpper }}_CA_ADMIN_CLIENT_PASSWORD={{ toLower $org.Name }}adminpw

# {{ $org.Name }} CA Postgres DB Credentials (if used)
{{ $orgUpper }}_CA_DB_USER=ca_admin
{{ $orgUpper }}_CA_DB_PASSWORD=ca_admin_pw

# {{ $org.Name }} CouchDB Credentials (if used)
{{ $orgUpper }}_COUCHDB_USER=admin
{{ $orgUpper }}_COUCHDB_PASSWORD=adminpw
{{- end }}

# pgAdmin Credentials (if used)
PGADMIN_DEFAULT_EMAIL=admin
PGADMIN_DEFAULT_PASSWORD=adminpw
